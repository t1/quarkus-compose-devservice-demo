quarkus:
  rest-client:
    fake-service:
      uri: http://localhost:8080/fake
    s3:
      # We already configure the port in the compose.yaml, so it would be nice to use that port here,
      # but it doesn't work, probably because that config is not yet available when this file is read.
      # uri: http://localhost:${quarkus.rest-client.s3.port}
      uri: http://localhost:9000
